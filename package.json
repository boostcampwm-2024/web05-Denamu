{
  "name": "web05-denamu",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@types/multer": "^1.4.13",
    "artillery": "^2.0.21",
    "commitizen": "^4.3.1",
    "cz-cli": "^1.0.0",
    "cz-customizable": "^7.2.1",
    "lefthook": "^1.8.2"
  },
  "scripts": {
    "prepare": "lefthook install",
    "commit": "cz",
    "stress-fe": "artillery run -o loadtest/FE_Report.json loadtest/FE_loadtest.yml",
    "stress-be": "artillery run -o loadtest/BE_Report.json loadtest/BE_loadtest.yml",
    "stress-chat": "artillery run -o loadtest/Chat_Report.json loadtest/Chat_loadtest.yml",
    "stress-fe-html": "artillery report loadtest/FE_Report.json",
    "stress-be-html": "artillery report loadtest/BE_Report.json",
    "stress-chat-html": "artillery report loadtest/Chat_Report.json",
    "start:dev": "docker compose -f docker-compose/docker-compose.dev.yml --profile default up --build --watch",
    "start:dev:was": "docker compose -f docker-compose/docker-compose.dev.yml --profile was up --build --watch",
    "start:dev:feed": "docker compose -f docker-compose/docker-compose.dev.yml --profile feed-crawler up --build --watch",
    "start:dev:email": "docker compose -f docker-compose/docker-compose.dev.yml --profile email-worker up --build --watch",
    "start:local": "docker compose -f docker-compose/docker-compose.local.yml --profile default up --build"
  },
  "config": {
    "commitizen": {
      "path": "cz-customizable"
    }
  }
}
